"""Dataset loaders (design §16.1).

Empty in v0 — :mod:`anvil.tasks.base.materialize_dataset` handles the simple
cases. The intent is for this package to host caching, dataset SHA
computation, and version-pinning helpers in v0.5+.
"""

from __future__ import annotations
