"""CaaS preflight self-healing agent (design §7).

M3 work — this package currently exposes no public API. The directory exists
so imports of subordinate stubs work and so the import-linter contracts can
be exercised against the eventual layout.
"""

from __future__ import annotations
